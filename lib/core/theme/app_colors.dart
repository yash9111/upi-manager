import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // BRAND

  static const primary = Color(0xFF0F766E);

  static const secondary = Color(0xFF14B8A6);

  static const accent = Color(0xFF5EEAD4);

  // SURFACES

  static const background = Color(0xFFF6F8F7);

  static const card = Colors.white;

  static const surface = Colors.white;

  static const border = Color(0xFFE5E7EB);

  // TEXT

  static const textPrimary = Color(0xFF0F172A);

  static const textSecondary = Color(0xFF64748B);

  // STATES

  static const success = Color(0xFF16A34A);

  static const warning = Color(0xFFF59E0B);

  static const danger = Color(0xFFDC2626);

  // SOFT SURFACES

  static const softPrimary = Color(0xFFDDF7F4);

  static const softSuccess = Color(0xFFDCFCE7);

  static const softWarning = Color(0xFFFEF3C7);

  static const softDanger = Color(0xFFFEE2E2);

  static const softBlue = Color(0xFFE0F2FE);

  static const softOrange = Color(0xFFFFEDD5);

  // DIVIDER / MUTED

  static const muted = Color(0xFF94A3B8);

  static const divider = Color(0xFFE2E8F0);

  // SHADOW

  static const shadow = Color(0x14000000);
}
